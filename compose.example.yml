services:
  agent:
    build: .
    volumes:
      - ./plow-credentials:/var/lib/plow/credentials:ro
      # The agent's home, kept across `up`/`down` so its sessions and setup
      # survive. It also shadows the image, so an edit to SOUL.md or a skill
      # is only picked up once it is gone: `docker compose down -v` starts
      # this agent fresh, and `down -v && up --build -d` is one edit round.
      - agent-home:/var/lib/hermes
    # No ports: the agent connects to Plow outbound.

volumes:
  agent-home:
