name: CLI smoke
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 tests/test-messages.py
      - run: python3 tests/smoke-line-in-use.py
      - run: python3 tests/smoke-deploy.py
      - run: python3 tests/smoke-agent-images.py
